import 'jest';
import { diaryFormModalState } from '../diary-form-modal.state';

describe('diary-form.modal.state', () => {
  it('should be created', () => {
    expect(diaryFormModalState).toBeDefined();
  });
});
